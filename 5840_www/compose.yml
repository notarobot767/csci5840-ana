---
services:
  www:
    command: [
      "--workers", "4",
      "--bind", "0.0.0.0:8080",
      "wsgi:app"
    ]
    environment:
      TZ: America/Denver
    build:
      target: www
      context: .
    ports:
      - 80:8080
    volumes:
      - ./app:/app:ro
    restart: unless-stopped
  webtest:
    environment:
      TZ: America/Denver
    build:
      target: webtest
      context: .
    ports:
      - 81:5000
    volumes:
      - ./app:/app:ro
    restart: unless-stopped