#################
# Cisco NetFlow #
#################
!
flow record flow
  match ipv4 source address
  match ipv4 destination address
  match ipv4 protocol
  match transport source-port
  match transport destination-port
  collect counter bytes long
  collect counter packets long
  collect flow direction
  collect interface input
  collect interface output
  collect ipv4 dscp
  collect ipv4 header-length
  collect ipv4 tos
  collect ipv4 ttl
  collect transport tcp flags
  collect ipv4 fragmentation flags
  collect transport icmp ipv4 type
  collect transport icmp ipv4 code
!
flow exporter flow
  description netflow receiver
  !destination 155.28.42.21
  !destination 22.225.42.13
  source loopback0
  transport udp 2055
  export-protocol ipfix
!
flow monitor flow
  exporter flow
  record flow
  cache timeout active 60
!
# apply on wan interface/tunnel
interface XXX
  ip flow monitor flow input
  ip flow monitor flow output